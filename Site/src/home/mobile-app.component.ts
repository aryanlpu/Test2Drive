import { Component } from '@angular/core';
import { Observable } from 'rxjs/Observable';

@Component({
    selector: 'ij-mobile-app',
    templateUrl: 'mobile-app.component.html',
    styleUrls: ['./styles/mobile-app.less']
})
export class MobileAppComponent {

    constructor (
    ) { }
}